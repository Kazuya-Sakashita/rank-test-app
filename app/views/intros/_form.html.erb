<%= form_with(model: intro) do |form| %>
  <% if intro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intro.errors.count, "error") %> prohibited this intro from being saved:</h2>

      <ul>
        <% intro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hometown %>
    <%= form.text_field :hometown %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
